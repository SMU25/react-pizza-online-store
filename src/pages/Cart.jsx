import React from "react";
import { useTranslation } from "react-i18next";

const T_PREFIX = "cart";

const HEADING = "title";

export const Cart = () => {
  const { t } = useTranslation();

  return <div>Cart</div>;
};
